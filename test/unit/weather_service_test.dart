import 'package:flutter_test/flutter_test.dart';

void main() {
  group(
    'WeatherService (legacy)',
    () {},
    skip: 'Legacy mkecitysmart-era test; WeatherService not in current app.',
  );
}
