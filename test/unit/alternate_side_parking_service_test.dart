import 'package:flutter_test/flutter_test.dart';

void main() {
  group(
    'AlternateSideParkingService (legacy)',
    () {},
    skip:
        'Legacy test targets an older AlternateSideParkingService API that no longer exists (clock/sideForDate/NotificationPriority).',
  );
}

