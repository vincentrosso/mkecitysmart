import 'package:flutter_test/flutter_test.dart';

void main() {
  group(
    'TicketApiService (legacy)',
    () {},
    skip: 'Legacy mkecitysmart-era test; TicketApiService not in current app.',
  );
}
