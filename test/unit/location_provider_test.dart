import 'package:flutter_test/flutter_test.dart';

void main() {
  group(
    'LocationProvider (legacy)',
    () {},
    skip: 'Legacy mkecitysmart-era test; LocationProvider/LocationService drift.',
  );
}

