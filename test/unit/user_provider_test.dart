import 'package:flutter_test/flutter_test.dart';

void main() {
  group(
    'UserProvider (legacy unit test)',
    () {},
    skip:
        'Legacy mkecitysmart-era unit test depends on old mocks/contracts; auth validated via on-device flows.',
  );
}