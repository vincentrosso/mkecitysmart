import 'package:flutter/material.dart';

class AppColors {
  // Backgrounds
  static const background = Color(0xFF021C1E); // deep green/blue
  static const mapBackground = Color(0xFF03292B); // slightly lighter
  static const card = Color(0xFF084238); // card tiles

  // Accents
  static const accentTeal = Color(0xFF1CC7AA); // teal markers
  static const accentOrange = Color(0xFFFF7A3C); // heat / parking highlight
  static const accentYellow = Color(0xFFFFCF5A); // P icons

  // Misc
  static const bottomNavIcon = Color(0xFFFFCF5A);
  static const mutedIcon = Color(0xFF5C7C76);
}
